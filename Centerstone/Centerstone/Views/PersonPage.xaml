<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Centerstone.PersonPage" xmlns:vm="clr-namespace:Centerstone;" Title="{Binding Title}">
    <!--<ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackLayout BackgroundColor="{StaticResource Accent}" VerticalOptions="FillAndExpand" HorizontalOptions="Fill">
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
                <ContentView Padding="0,40,0,40" VerticalOptions="FillAndExpand">
                  
                </ContentView>
            </StackLayout>
        </StackLayout>
        <ScrollView Grid.Row="1">
            <StackLayout Orientation="Vertical" Padding="16,40,16,40" Spacing="10">
                <Label Text="Gender" FontSize="Medium" />
                <!--<Entry Text="{Binding Item.Text}" FontSize="Small" />-->
                <Label Text="Relation To Primary" FontSize="Medium" />
                <!--<Editor Text="{Binding Item.Description}" FontSize="Small" Margin="0" />-->
                <Label Text="Race" FontSize="Medium" />
                <Label Text="Ethnicity" FontSize="Medium" />
                <Label Text="Education (24 Years or Order)" FontSize="Medium" />
                <Label Text="Included In Calucation" FontSize="Medium" />
                <Label Text="Disabled" FontSize="Medium" />
                <Label Text="Military Veteran" FontSize="Medium" />
                <Label Text="Health Insurance" FontSize="Medium" />
            </StackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
