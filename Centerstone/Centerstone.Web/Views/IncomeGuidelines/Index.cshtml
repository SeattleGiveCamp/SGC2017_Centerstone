@model IEnumerable<Centerstone.Models.IncomeGuideline>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.HouseholdSize)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.MaxIncome)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HouseholdSize)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaxIncome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.myKey">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.myKey">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.myKey">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
